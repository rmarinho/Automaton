<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!-- Required for MauiDevFlow agent to accept incoming connections -->
        <key>com.apple.security.network.server</key>
        <true/>
        <!-- NOTE: For SecureStorage (Keychain) support, add keychain-access-groups
             and configure code signing with a provisioning profile:
             <key>keychain-access-groups</key>
             <array>
               <string>$(AppIdentifierPrefix)com.companyname.automatondesigner</string>
             </array>
             Without it, SettingsService falls back to Preferences storage. -->
    </dict>
</plist>

